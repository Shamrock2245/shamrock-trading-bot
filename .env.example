# ─────────────────────────────────────────────────────────────────────────────
# Shamrock Trading Bot — Environment Variables Template
# Copy this file to .env and fill in real values.
# NEVER commit .env to git. It is gitignored.
# ─────────────────────────────────────────────────────────────────────────────

# ── Trading Mode ─────────────────────────────────────────────────────────────
# paper = simulate trades only (no real funds moved)
# live  = real execution (⚠️ only after thorough paper testing)
MODE=paper

# ── EVM Wallet Private Keys (NEVER hardcode — use env vars only) ──────────────
# Primary wallet: gem sniping & active positions
WALLET_PRIVATE_KEY_PRIMARY=your_primary_wallet_private_key_here

# Wallet B: DCA & mean-reversion strategies
WALLET_PRIVATE_KEY_B=your_wallet_b_private_key_here

# Wallet C: long-term holds & profit sweeps (cold storage)
WALLET_PRIVATE_KEY_C=your_wallet_c_private_key_here

# ── Solana Wallet Keys (base58-encoded keypairs) ──────────────────────────────
# Primary Solana wallet (used for Solana gem sniping via Jupiter)
SOLANA_PRIVATE_KEY_PRIMARY=your_solana_primary_base58_private_key_here

# Wallet B Solana key (optional — for DCA on Solana)
SOLANA_PRIVATE_KEY_B=your_solana_b_base58_private_key_here

# Wallet C Solana key (optional — cold reserve on Solana)
SOLANA_PRIVATE_KEY_C=your_solana_c_base58_private_key_here

# ── EVM Wallet Public Addresses (safe to reference) ───────────────────────────
WALLET_ADDRESS_PRIMARY=0x0000000000000000000000000000000000000001
WALLET_ADDRESS_B=0x0000000000000000000000000000000000000002
WALLET_ADDRESS_C=0x0000000000000000000000000000000000000003

# ── Solana Wallet Public Addresses (base58) ───────────────────────────────────
# Your Solana wallet public keys (safe to reference)
SOLANA_ADDRESS_PRIMARY=your_solana_primary_public_key_here
SOLANA_ADDRESS_B=your_solana_b_public_key_here
SOLANA_ADDRESS_C=your_solana_c_public_key_here

# ── EVM RPC Endpoints ─────────────────────────────────────────────────────────
# Ethereum (use Alchemy/Infura for reliability — free tier available)
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your_alchemy_key
ETH_RPC_FALLBACK=https://cloudflare-eth.com

# Base
BASE_RPC_URL=https://mainnet.base.org
BASE_RPC_FALLBACK=https://base.publicnode.com

# Arbitrum
ARB_RPC_URL=https://arb1.arbitrum.io/rpc
ARB_RPC_FALLBACK=https://arbitrum.publicnode.com

# Polygon
POLYGON_RPC_URL=https://polygon-rpc.com
POLYGON_RPC_FALLBACK=https://polygon.publicnode.com

# BSC
BSC_RPC_URL=https://bsc-dataseed.binance.org
BSC_RPC_FALLBACK=https://bsc.publicnode.com

# ── Solana RPC ────────────────────────────────────────────────────────────────
# Use a dedicated RPC for speed (Helius, QuickNode, or Triton recommended)
# Free public RPC: https://api.mainnet-beta.solana.com (rate-limited)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# Helius (recommended for production): https://rpc.helius.xyz/?api-key=YOUR_KEY
# SOLANA_RPC_URL=https://rpc.helius.xyz/?api-key=your_helius_api_key

# ── Jupiter Aggregator (Solana DEX routing) ───────────────────────────────────
JUPITER_API_URL=https://api.jup.ag/swap/v1

# ── MEV Protection ────────────────────────────────────────────────────────────
# Flashbots relay endpoint (Ethereum mainnet)
FLASHBOTS_RPC_URL=https://rpc.flashbots.net
# Flashbots signing key (separate from trading keys — used to sign bundles)
FLASHBOTS_SIGNING_KEY=your_flashbots_signing_key_here

# CoW Protocol (MEV-protected swaps via batch auctions)
COW_API_URL=https://api.cow.fi/mainnet

# ── DEX API Keys ──────────────────────────────────────────────────────────────
# 1inch Aggregation API
ONEINCH_API_KEY=your_1inch_api_key_here
ONEINCH_API_URL=https://api.1inch.dev/swap/v6.0

# ── Data Provider API Keys ────────────────────────────────────────────────────
# CoinMarketCap
CMC_API_KEY=your_coinmarketcap_api_key_here

# CoinGecko (leave blank for free tier)
COINGECKO_API_KEY=

# Etherscan (and compatible explorers)
ETHERSCAN_API_KEY=your_etherscan_api_key_here
BASESCAN_API_KEY=your_basescan_api_key_here
ARBISCAN_API_KEY=your_arbiscan_api_key_here
POLYGONSCAN_API_KEY=your_polygonscan_api_key_here
BSCSCAN_API_KEY=your_bscscan_api_key_here

# Moralis (Pro subscription — Data API + Streams)
MORALIS_API_KEY=your_moralis_api_key_here

# ── Moralis Streams (push-based webhooks for real-time events) ────────────────
# Streams deliver events in 1-2s vs 30s+ polling — critical for copy-trading latency
MORALIS_STREAMS_ENABLED=true
MORALIS_STREAMS_HOST=0.0.0.0
MORALIS_STREAMS_PORT=8787
MORALIS_STREAMS_WEBHOOK_SECRET=your_webhook_secret_here
# Public URL that Moralis can reach (e.g. your Hetzner IP + port)
MORALIS_STREAMS_WEBHOOK_URL=http://46.62.231.43:8787/moralis/streams
MORALIS_STREAMS_AUTO_SYNC=true
MORALIS_STREAMS_HEALTH_INTERVAL=300

# Stream feature flags
MORALIS_STREAMS_WHALE_ENABLED=true
MORALIS_STREAMS_WHALE_MIN_USD=50000
MORALIS_STREAMS_LIQUIDITY_ENABLED=true
MORALIS_STREAMS_SOLANA_DISCOVERY_ENABLED=true
MORALIS_STREAMS_SOLANA_ALPHA_ENABLED=true
MORALIS_STREAMS_FALLBACK_POLL_INTERVAL=120

# Solana smart money wallets for copy-trading (comma-separated)
SOLANA_SMART_MONEY_WALLETS=

# Token Sniffer
TOKEN_SNIFFER_API_KEY=your_tokensniffer_api_key_here

# LunarCrush (social sentiment — free tier: 100 req/day)
# Sign up at: https://lunarcrush.com/developers
LUNARCRUSH_API_KEY=your_lunarcrush_api_key_here

# GoPlus Security API (token safety checks — free tier)
# No key required for free tier: https://gopluslabs.io/
GOPLUS_API_KEY=

# ── Smart Money Tracking ──────────────────────────────────────────────────────
# Comma-separated list of known profitable wallet addresses to track
# These are checked against token holders to score smart money overlap
# Example: Alex Becker's known trading wallets, top Zerion wallets, etc.
SMART_MONEY_WALLETS=0x0000000000000000000000000000000000000001,0x0000000000000000000000000000000000000002

# ── Notifications ─────────────────────────────────────────────────────────────
# Slack webhook URL (create at api.slack.com/apps)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/here
SLACK_CHANNEL_TRADES=#shamrock-trades
SLACK_CHANNEL_ALERTS=#shamrock-alerts

# Telegram bot (create via @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# ── Social Insider Oracle (ECC Skill: sentiment-insider-oracle) ───────────────
# Minimum social mention velocity (per 5 minutes) to trigger pre-volume sniper.
ORACLE_VELOCITY_THRESHOLD_5M=10
# Minimum KOLs shilling simultaneously to flag as insider cabal pump.
ORACLE_CABAL_KOL_THRESHOLD=3
# Time window (seconds) for cabal detection — default 5 minutes.
ORACLE_CABAL_WINDOW_S=300
# Comma-separated list of additional KOL Twitter handles to track (no @ prefix).
# Example: KOL_HANDLES_LIST=ansem,crash,gcr,blknoiz06
KOL_HANDLES_LIST=
# Position size multiplier for oracle sniper entries (0.5 = half normal size).
ORACLE_SNIPER_SIZE_MULTIPLIER=0.5

# ── Risk Management Parameters ────────────────────────────────────────────────
# Maximum % of wallet to risk per trade
MAX_POSITION_SIZE_PERCENT=2.0

# Maximum concurrent open positions per wallet
MAX_CONCURRENT_POSITIONS=10

# Trailing stop-loss from peak price (%) — activated after TP1
STOP_LOSS_PERCENT=20.0

# Hard stop-loss from entry price (%)
HARD_STOP_LOSS_PERCENT=25.0

# Take-profit levels (multipliers from entry)
# TP1 at 2x → sell 40% | TP2 at 5x → sell 35% | TP3 at 10x → sell 20%
TAKE_PROFIT_1_MULTIPLIER=2.0
TAKE_PROFIT_2_MULTIPLIER=5.0
TAKE_PROFIT_3_MULTIPLIER=10.0

# Circuit breaker: halt all trading if portfolio drops this % in 24h
CIRCUIT_BREAKER_PERCENT=15.0

# Daily loss limit per wallet (in ETH)
DAILY_LOSS_LIMIT_ETH=0.5

# Maximum gas price (gwei) — skip trades above this
MAX_GAS_GWEI=50

# Minimum ETH balance alert threshold (can't pay gas below this)
MIN_ETH_BALANCE_ALERT=0.05

# ── Scanner Settings ──────────────────────────────────────────────────────────
# How often to run the gem scanner (seconds)
SCAN_INTERVAL_SECONDS=60

# Minimum gem score to consider (0-100)
# Lowered from 65 to 55 to catch more early gems
MIN_GEM_SCORE=55

# Express lane threshold — skip TA, execute immediately (score ≥ this)
EXPRESS_LANE_SCORE=82

# Minimum liquidity in USD
MIN_LIQUIDITY_USD=50000

# Maximum token age to consider (hours)
MAX_TOKEN_AGE_HOURS=168

# Active chains to scan (comma-separated)
# Options: ethereum, base, arbitrum, polygon, bsc, solana
# CURRENT STRATEGY: Base + Arbitrum + Solana only.
# Ethereum excluded — high gas + heavy Moralis CU burn (alpha-wallet streams fire hundreds of events/hr on ETH).
# BSC + Polygon excluded — low alpha vs CU cost. Re-enable when capital and CU budget allow.
ACTIVE_CHAINS=base,arbitrum,solana

# ── Position Monitor ──────────────────────────────────────────────────────────
# How often to check open positions for TP/SL (seconds)
POSITION_CHECK_INTERVAL_SECONDS=30

# Output files for position persistence
POSITIONS_FILE=output/positions.json
TRADES_FILE=output/trades.json

# ── Conviction-Based Position Sizing ─────────────────────────────────────────
# Score thresholds for position size multipliers
CONVICTION_HIGH_THRESHOLD=82   # Express lane: 100% of max size
CONVICTION_MID_THRESHOLD=70    # Strong signal: 75% of max size
# Below 70: 50% of max size

CONVICTION_HIGH_MULTIPLIER=1.0
CONVICTION_MID_MULTIPLIER=0.75
CONVICTION_LOW_MULTIPLIER=0.5

# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=sqlite:///./data/shamrock_trading.db

# ── Helius (Enhanced Solana RPC — bundle detection, Jito integration) ─────────
# Sign up at: https://dev.helius.xyz/
HELIUS_API_KEY=your_helius_api_key_here

# ── OpenAI (LLM Auto-Tuner — trailing-stop / TaG agent) ───────────────────────
# Secret key from platform.openai.com (project key is fine). Required for auto-tuner.
OPENAI_API_KEY=sk-proj-your_openai_api_key_here
# Prefer free daily high-volume models when "Share inputs and outputs" is enabled:
#   gpt-5-mini | gpt-5.4-mini | gpt-4o-mini  (~2.5M tok/day free class)
# Paid frontier (billed at standard rates): gpt-5.6-luna | gpt-5.6-terra | gpt-5.6-sol
OPENAI_MODEL=gpt-5-mini

# ── Grok / xAI (X platform sentiment scoring — 5% gem score weight) ───────────
GROK_API_KEY=your_grok_api_key_here
# Grok rate limits — set these in your xAI console
# Recommended: RPM=60, TPM=100000
GROK_RPM=60
GROK_TPM=100000
# CRO Agent — adversarial risk check before execution (set to false to disable)
CRO_AGENT_ENABLED=true
# MiroFish Lite — swarm simulation every 10 cycles (set to false to disable)
MIROFISH_ENABLED=true

# ── Jito (Solana MEV Protection — bundle submissions) ─────────────────────────
JITO_BLOCK_ENGINE_URL=https://mainnet.block-engine.jito.wtf/api/v1/bundles
JITO_AUTH_KEY=your_jito_auth_keypair_here

# ── Hyperliquid (Perpetual Futures DEX — zero gas, leveraged trading) ─────────
HYPERLIQUID_ENABLED=true
HYPERLIQUID_WALLET_ADDRESS=your_hyperliquid_evm_address_here
HYPERLIQUID_PRIVATE_KEY=your_hyperliquid_private_key_here
HYPERLIQUID_USE_TESTNET=false
HYPERLIQUID_DEFAULT_LEVERAGE=3
HYPERLIQUID_MAX_POSITION_USD=150
# Notional cap (margin × leverage). 10 slots × $400 notional ≈ $4000; leave headroom.
HYPERLIQUID_MAX_TOTAL_EXPOSURE=5000
HYPERLIQUID_STOP_LOSS_PCT=2.5
HYPERLIQUID_TAKE_PROFIT_PCT=12.0
HYPERLIQUID_MAX_POSITIONS=10  # Concurrent slots for $500/day rotation
HYPERLIQUID_DAILY_LOSS_LIMIT=100.0
# MUST match HL_PERPS_EXEC_SCORE — executor rejects scores below this after scanner approval
HYPERLIQUID_MIN_GEM_SCORE=55
# Scanner-side gates — tuned 2026-07-17 from trade_history (26).csv
# Edge is real post-fix (41% WR, 3.26x R:R, +$28.76 since Jul 13) but volume ~3.5 opens/day
# LONG_ONLY: keep true — shorts ~17% WR in live book; re-enable only after short edge proven
HL_PERPS_LONG_ONLY=true
# MIN_RR: keep 1.2 — quality is working (do not re-open 1.5 starvation or 1.1 looseness)
HL_PERPS_MIN_RR=1.2
HL_PERPS_MAX_NOTIONAL_USD=400
HL_PERPS_MAX_MARGIN_EQUITY_PCT=0.15
HL_PERPS_MAX_POSITIONS=10
HL_PERPS_MAX_POSITION_USD=150
# Cooldowns: 8/10/60 unstack volume gates; RSI veto + autoban still block micro-churn
HL_PERPS_REENTRY_COOLDOWN_MIN=8
HL_PERPS_EMERGENCY_COOLDOWN_MIN=60
HL_PERPS_LOSS_COOLDOWN_MIN=10
# Toxic coins: live losers + HYPE (0% WR / 4 trades in trade_history 26)
# trade_history (27): TRB/GRASS/EIGEN/MET worst; LIT/JTO added
# trade_history (28): KAITO; (29): HEMI 0% WR, ONDO −$18 one-shot
HL_PERPS_TOXIC_COINS=AAVE,HMSTR,GRASS,EIGEN,POPCAT,MORPHO,BRETT,APE,MET,MEME,FARTCOIN,TRB,HYPE,LIT,JTO,KAITO,HEMI,ONDO
HL_PERPS_TOXIC_SCORE_BONUS=20
# Skip new entries during worst ET hours from trade_history(27); empty disables
HL_PERPS_BLOCKED_HOURS_ET=10,17,22
HL_PERPS_MAX_TOTAL_EXPOSURE=5000
# EXEC_SCORE: 55 base (was 58); goal engine shifts to floor 52 when behind $500 pace
HL_PERPS_EXEC_SCORE=55
HL_TRAILING_ROE_TRIGGER_PCT=10.0
HL_TRAILING_DISTANCE_PCT=1.0
# Auto-blacklist: dynamic performance-based ban (bans coins with <30% WR after 5 trades)
HL_PERPS_AUTOBAN_ENABLED=true
HL_PERPS_AUTOBAN_MIN_TRADES=5
HL_PERPS_AUTOBAN_WR_THRESHOLD=0.30
HL_PERPS_AUTOBAN_HOURS=48.0
# Winning risk (trade_history 29) — supersedes profit_lock when enabled
# BE@0.75%, TP1 50%@+2%, trail 0.5%, 30-min −1% tighten, 2h hard timeout
WINNING_RISK_MANAGER_ENABLED=true
WINNING_ENTRY_FILTER_ENABLED=true
FAST_BREAK_EVEN_PCT=0.75
FAST_BREAK_EVEN_SL_OFFSET=0.05
TP1_PROFIT_PCT=2.0
TP1_SIZE_PCT=50
MIN_RULE_TIME_MINUTES=30
MIN_RULE_SL_OFFSET=-1.0
TRAILING_STOP_PCT=0.5
WINNING_LOSS_TIMEOUT_HOURS=2.0
TOXIC_ZONE_RESTRICTION=true
TOXIC_ZONE_START=8
TOXIC_ZONE_END=14
TOXIC_ZONE_SIZE_MULT=0.5
MIN_VOLUME_USD=1000000
# Fallback profit-lock if Winning is disabled
PROFIT_LOCK_ENABLED=true
PROFIT_LOCK_BE_PCT=1.5
PROFIT_LOCK_BE_BUFFER_PCT=0.1
PROFIT_LOCK_TRAIL_ACTIVATE_PCT=3.0
PROFIT_LOCK_TRAIL_DISTANCE_PCT=1.5
PROFIT_LOCK_LOSS_TIMEOUT_HOURS=4.0
# HF Moralis scanner: STUB only (returns empty). Keep false until real API wired.
HF_MORALIS_SCANNER_ENABLED=false
HF_SCANNER_INTERVAL=300

# ── Observability ─────────────────────────────────────────────────────────────
# Sentry DSN for production error tracking (free tier: 5K errors/month)
# Create project at: https://sentry.io → Settings → Projects → Create
SENTRY_DSN=

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
LOG_DIR=./logs

# ── Zero-Risk Flash Arb Engine ─────────────────────────────────────────────────
# FlashArbReceiver contract addresses (deploy contracts/FlashArbReceiver.sol first)
# See docs/FLASH_ARB_ENGINE.md for deployment instructions
FLASH_ARB_RECEIVER_ETHEREUM=
FLASH_ARB_RECEIVER_BASE=
FLASH_ARB_RECEIVER_ARBITRUM=
FLASH_ARB_RECEIVER_POLYGON=
FLASH_ARB_RECEIVER_BSC=
FLASH_ARB_RECEIVER_AVALANCHE=
# Minimum net profit % required to execute (default: 1.5%)
FLASH_ARB_MIN_PROFIT_PCT=1.5
# Hard cap on flash loan size in USD (default: $500,000)
FLASH_ARB_MAX_POSITION_USD=500000.0
# Max fraction of DEX pool liquidity to borrow (default: 30%)
FLASH_ARB_LIQUIDITY_FRACTION=0.30
# Safety haircut on minExpectedProfit passed to on-chain contract (default: 10%)
FLASH_ARB_SAFETY_MARGIN_PCT=0.10
# Prefer Balancer V2 (0% fee) over Aave V3 (0.05% fee) — default: true
FLASH_ARB_PREFER_BALANCER=true
