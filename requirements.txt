# ─────────────────────────────────────────────────────────────────────────────
# Shamrock Trading Bot — Python Dependencies
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Blockchain / Web3 (EVM) ───────────────────────────────────────────────────
web3>=6.0.0                  # Ethereum/EVM chain interaction
eth-account>=0.10.0          # Account management (sign txns from env vars)
hyperliquid-python-sdk>=0.22.0  # Hyperliquid perps DEX SDK (zero-gas leveraged trading)

# ── Blockchain / Web3 (Solana) ────────────────────────────────────────────────
# Solana trade execution via Jupiter aggregator
solders>=0.20.0              # Solana Python SDK (keypair, transaction signing)
solana>=0.32.0               # Solana JSON-RPC client
base58>=2.1.1                # Base58 encoding/decoding for Solana keys

# ── HTTP / Async ─────────────────────────────────────────────────────────────
requests>=2.31.0             # Synchronous HTTP (DexScreener, GoPlus, etc.)
aiohttp>=3.9.0               # Async HTTP for concurrent API calls
websockets>=11.0.3           # WebSocket client for Mempool/Streams

# ── Data / Analysis ──────────────────────────────────────────────────────────
pandas>=2.0.0                # DataFrames for OHLCV and signal processing
# pandas-ta — optional, all indicators have manual fallback calculations
# PyPI version broken on Python 3.11+; git version fails in Docker
# Bot works fully without it via strategies/indicators.py fallbacks
# pandas-ta @ git+https://github.com/twopirllc/pandas-ta.git@development
numpy>=1.24.0                # Numerical operations
networkx>=3.1                # Graph algorithms for Bellman-Ford triangular arb detection

# ── Machine Learning ─────────────────────────────────────────────────────
xgboost>=2.0.0               # XGBoost classifier for ML weight optimization
scikit-learn>=1.3.0          # StandardScaler, cross_val_score for ML pipeline
optuna>=4.0.0                # Bayesian hyperparameter optimization (TPE, NSGAIIISampler)
optuna-dashboard             # Interactive web dashboard for optimization studies
openai>=1.0.0                # LLM Auto-Tuning via OpenAlice reasoning architecture

# ── Configuration ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0         # Load .env file into environment variables

# ── Scheduling ───────────────────────────────────────────────────────────────
APScheduler>=3.10.0          # Background job scheduler for scan loops

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0            # ORM for trade/position persistence
alembic>=1.12.0              # Database migrations

# ── Notifications ────────────────────────────────────────────────────────────
slack-sdk>=3.21.0            # Slack alerts
python-telegram-bot>=20.0    # Telegram bot alerts

# ── Dashboard ────────────────────────────────────────────────────────────────
reflex>=0.4.0                # Premium React/Next.js UI dashboard

# ── Observability ─────────────────────────────────────────────────────────────
sentry-sdk>=2.0.0            # Production error tracking & performance monitoring
py-spy>=0.3.14               # Live flamegraph profiling for running bot process

# ── Utilities ────────────────────────────────────────────────────────────────
colorama>=0.4.6              # Colored terminal output
tabulate>=0.9.0              # Pretty-print tables in console
tenacity>=8.2.0              # Retry logic with exponential backoff
pydantic>=2.0.0              # Data validation for API responses

# ── Cosmos Ecosystem ─────────────────────────────────────────────────────
bech32>=1.2.0                 # Bech32 address encoding (Cosmos addresses)
grpcio>=1.60.0                # gRPC for Cosmos chain communication
protobuf>=4.25.0              # Protocol buffers for Cosmos TX signing

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0

# ── GMGN.ai Ed25519 Auth ─────────────────────────────────────────────────────
cryptography>=42.0.0
openai>=1.0.0
